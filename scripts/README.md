# scripts/

Developer convenience scripts.

| Script | Purpose |
|---|---|
| `bootstrap.sh` / `bootstrap.ps1` | One-time setup: env files, venv, deps, pre-commit hooks. |

More operational scripts (seed data, DB reset) are added as later stages need them.
